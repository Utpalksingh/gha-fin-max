name: workflow using reuseable workflow
on:
  push:
    branches:
      - main
jobs:
  call_reuseable_workflow:
    # uses: ./.github/workflows/reuseable-workflow.yml
    uses: Utpalksingh/gh-practise-project/.github/workflows/reuseable-workflow.yml@main
