name: hello world
on:
  push: 
    branches:
      - main
      - develop
      - feature/*
      - 'release/*'
      - 'hotfix/*'
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main
      - develop
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: deploy job
        run: echo "job deployed"
  
    
